[Back to Top](index)

# Functions

They start with a `fn`, no `end` but need the type specifications in the
parameter list

```rust
fn call_me(num: i32) {
    println!("Ring! Call number {}", i + 1);
}
```
